import React from "react";

function Title() {
  return (
    <header style={titleStyle}>
      <h1>Grocery List </h1>
    </header>
  );
}

const titleStyle = {
  background: "silver"
};

export default Title;
